# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: idp-app
metadata:
  template: idp-app@1.0.0
services:
  api:
    project: .
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile
      remoteBuild: true
infra:
  provider: bicep
  path: infra
  module: main
